## 2026-07-12

- 配置 Git 双分支策略：`main` 跟踪上游，`annotated` 用于源码注释
- 添加 upstream 远程：https://github.com/vllm-project/vllm.git
- 创建 annotated 分支并推送至 origin fork
- 完成 vLLM 混合注意力（Full + Sliding Window）KV 缓存机制深度分析，文档位于 `.codebuddy/analysis/hybrid_attention_kv_cache.md`
- 按团队注释规范给 5 个核心源码文件加中文注释（零 lint 错误）：
  - `kv_cache_manager.py` — KVCacheManager 门面类 + allocate_slots 5 阶段注释
  - `kv_cache_coordinator.py` — HybridKVCacheCoordinator 前缀缓存交集算法
  - `single_type_kv_cache_manager.py` — Full/SlidingWindow 前缀扫描 + 基类完整注释(接口清单+调用链①~⑦+子类对比表)
  - `kv_cache_utils.py` — 混合模型 KV cache 分组逻辑
  - `flex_attention.py` — per-layer 切换 + mask 组合管道
