# 2026-07-14

## v0.25.1 注释分支修复 (branch: comments-on-v0.25.1)

- 文件 `vllm/v1/core/single_type_kv_cache_manager.py` 之前被合并脚本破坏：`__init__` 丢了 8 个关键属性赋值（scheduler_block_size / block_size / kv_cache_spec / block_pool / enable_caching / _max_admission_blocks_per_request / new_block_ids / req_to_blocks），并错塞了 main 专属的 CoW docstring。已按 v0.25.1 tag 还原。
- 用户的中文注释原本写在 **main** 分支（含 CoW partial-hit、KV zeroing、needs_kv_cache_zeroing、`_record_new_block_ids`、`allocate_new_computed_blocks`、`take_pending_cow_copies`、`_partial_hit_reqs`、`_pending_cow_copies` 等 v0.25.1 不存在的特性）。
- 用户决策（2026-07-14）：清理掉 main 专属内容，让注释精确匹配 v0.25.1。已清理 8 处（类 docstring 接口表/调用链/数据结构 + `get_num_blocks_to_allocate`/`allocate_new_blocks` 的 docstring）。保留合法内容（EAGLE/MTP/`use_eagle`/`drop_eagle_block`、`reachable_block_mask`、`new_block_ids` 仅作"已分配 block 记录"用，无 zeroing）。
- 修复后校验：相对 v0.25.1 **代码级 diff=0**，语法 OK，lint 0 错误。
- 注意：v0.25.1 的 `new_block_ids`/`take_new_block_ids` 仅用于"记录/拉取自上次调用后新分配的 block"，**没有 zeroing 语义**（zeroing 是 main 才加的）。
- 用户拒绝了清理 `.codebuddy/tmp/` 目录，保留不动。
