# 2026-07-13 工作记录

## KV Cache 源码分析（annotated 分支）

### 已读文件
- `vllm/v1/core/single_type_kv_cache_manager.py` — FullAttentionManager / SlidingWindowManager / MambaManager 等
- `vllm/v1/core/kv_cache_coordinator.py` — KVCacheCoordinator / HybridKVCacheCoordinator
- `vllm/v1/core/kv_cache_manager.py` — allocate_slots 核心流程
- `vllm/v1/core/sched/scheduler.py` — schedule() 与 KV cache 交互点
- `vllm/v1/core/kv_cache_utils.py` — BlockHashListWithBlockSize 链式 hash 映射

### 深入理解的知识点
1. BlockHashListWithBlockSize：链式 hash 原理，把细粒度 hash 映射到粗粒度块
2. SWA find_longest_cache_hit 的两种命中路径：满命中 + 短请求部分命中
3. VLLM_PREFIX_CACHE_RETENTION_INTERVAL：SWA/Mamba 稀疏缓存保留开关
4. allocate_slots 三阶段分配 + 准入控制
5. 各 group block pool 独立 → 加法（非 max）汇总需求
6. drop_eagle_block 原因：KV cache 不存 hidden states，draft head 需要最后 block 重算

### 代码注释
- SWA `find_longest_cache_hit` 加入三种命中路径注释
- `CrossAttentionManager` 一次性静态分配注释
- `get_num_blocks_to_allocate` 加法原理注释

### 产出报告
- `.codebuddy/analysis/` 待补充... Scheduler ↔ KVCacheManager 交互报告
